name: growth_core
description: Shared building blocks (result, errors, ads, review, network, theme) reused across the Flutter apps in this workspace.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.3.0 <4.0.0"
  flutter: ">=3.19.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.5.0
  dio: ^5.4.0
  connectivity_plus: ^6.0.0
  shared_preferences: ^2.2.0
  google_mobile_ads: ^5.1.0
  in_app_review: ^2.0.9

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
